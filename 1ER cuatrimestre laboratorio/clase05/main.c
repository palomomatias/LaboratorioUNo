#include <stdio.h>
#include <stdlib.h>
#include "funcionesVectores.h"
/*

para definir un vector tiene que tener espacio de memoria reservado [] y lo que hace es despues separar ese espacio.
Se puede inicializar directamente siempre y cuando se inicializa cuando lo definimos.
subconjunto tiene 2 reglas, tiene que ser del mismo tipo y la misma cantidad
** carga secuencial se ult


*/

int main()
{
    int indice[5];
    int auxiliar;
    int i;
    float listaPrecio;
    indice=dameEnteroEntre[0,4];
    auxiliar=dameFlotanteEntre[1,1000];
    listaPrecio[indice]=auxiliar;

  for(i=0;i<5;i++)
  {
      printf("%f\n",listaPrecios[i]);
  }

    return 0;
}
